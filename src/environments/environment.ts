export const environment = {
  production: false,
  // Local ASP.NET Core API (see launchSettings.json). Run `dotnet dev-certs https --trust`
  // once so the browser trusts the dev certificate. Change to your ngrok/hosted URL if needed.
  apiUrl: 'https://localhost:63615'
};
